{
  "compilerOptions": {
    /* 用来指定允许从没有默认导出的模块中默认导入 */
    "allowSyntheticDefaultImports": true,
    /* declaration的值为true或false，用来指定是否在编译的时候生成相应的".d.ts"声明文档。
      如果设为true，编译每个ts文档之后会生成一个js文档和一个声明文档。
      但是declaration和allowJs不能同时设为true
    */
    "declaration": true,
    /* 输出代码 ES 版本，可以是 ["es3", "es5", "es2015", "es2016", "es2017", "esnext"] */
    "target": "esnext",
    /* 模块解析策略 */
    "moduleResolution": "node",
    "jsx": "react",
    "resolveJsonModule": true,
    "strict": true,
    "noImplicitAny": false,
    /* TS 需要引用的库，即声明文件 */
    "lib": ["dom", "esnext"],
    "module": "esnext",
    "esModuleInterop": true
  }
}
